"""Application layer for Py2048.

This package contains UI-agnostic models and logic for handling user interaction,
menu flow, and application state transitions. It acts as a bridge between the
core game logic (in `core/`) and the various user interfaces (in `interfaces/`).
"""
